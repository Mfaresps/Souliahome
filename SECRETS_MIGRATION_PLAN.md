# خطة نقل الأسرار خارج الريبو — SOULIA

> **الحالة: لم تُنفّذ بعد.** هذا مستند تخطيط فقط. لم يتم تغيير أي سرّ حيّ.
> نُفّذ في Aug 6, 2026 كمخرج من مراجعة أمنية؛ ينبغي تنفيذه خارج ساعات العمل.

---

## ١. ما هي المشكلة بالضبط

### أ) أسرار مكتوبة في ملف مرفوع على Git

في [docker-compose.yml](docker-compose.yml):

| السطر | المتغير | القيمة |
|---|---|---|
| 8 | `MONGO_INITDB_ROOT_PASSWORD` | `soulia_secret_2024` |
| 25 | `MONGODB_URI` | يحتوي نفس كلمة السر |
| 26 | `JWT_SECRET` | `soulia_jwt_super_secret_key_2024` |

### ب) الأخطر: سرّ احتياطي مكتوب داخل كود المصدر

```
backend/src/auth/auth.module.ts:26        || 'soulia_jwt_default_secret'
backend/src/auth/strategies/jwt.strategy.ts:17  || 'soulia_jwt_default_secret'
```

هذا **أخطر من (أ)**. لو فشل تمرير `JWT_SECRET` لأي سبب (خطأ إملائي، متغير ناقص بعد النقل)،
لا يتوقف النظام ولا يُصدر تحذيراً — بل **يعمل بصمت بسرّ معروف للجميع في الكود**.

### لماذا `JWT_SECRET` تحديداً هو الأخطر

من يملك السرّ يستطيع **توقيع توكن لأي مستخدم بأي صلاحية**، بما فيها `admin`.
هذا يتجاوز:
- نظام صلاحيات `categories-*` بالكامل
- `PermsGuard` و `RolesGuard` (كلاهما يثق في التوكن بعد التحقق من التوقيع)
- أي حماية أخرى مبنية على هوية المستخدم

لا يحتاج المهاجم كلمة سر أي موظف — يحتاج السرّ فقط.

---

## ٢. نطاق الأثر (مهم قبل التنفيذ)

| التغيير | الأثر المباشر |
|---|---|
| تغيير `JWT_SECRET` | **كل المستخدمين يُخرجون فوراً** ويحتاجون تسجيل دخول جديد. لا فقد بيانات. |
| تغيير كلمة سر Mongo | يتطلب تغييرها **داخل قاعدة البيانات الحيّة أيضاً**، وإلا يفشل الاتصال. |

`JWT_EXPIRES_IN` غير معرّف في compose، أي أن التوكنات صالحة **٢٤ ساعة** (الافتراضي في
`auth.module.ts:27`). لا توجد آلية refresh — لذا الخروج الجماعي حدث عادي نسبياً، لكن يجب
أن يقع في وقت مناسب.

> ⚠️ **لا تُغيّر السرّين في نفس الخطوة.** إن فشل شيء، لن تعرف أيّهما السبب.

---

## ٣. خطة التنفيذ

### المرحلة ١ — إزالة الـ fallback من الكود (آمنة تماماً، نفّذها أولاً)

هذه **لا تُخرج أحداً** ولا تغيّر أي سرّ. تجعل النظام يرفض العمل بسرّ افتراضي بدل أن
يقبله بصمت:

في `auth.module.ts` و `jwt.strategy.ts`، استبدل الـ `|| 'soulia_jwt_default_secret'` بما
يرمي خطأً واضحاً عند الإقلاع إذا كان المتغير غائباً (fail fast بدل fail silent).

بعدها: أعد تشغيل الباك-إند وتأكد أنه يعمل طبيعياً (السرّ ما زال ممرَّراً من compose).
**هذه المرحلة وحدها تُزيل أخطر جزء من المشكلة.**

### المرحلة ٢ — نقل القيم إلى `.env` على السيرفر

`.gitignore` عندك **سليم بالفعل** ويستثني `.env` و `.env.*` — لا يحتاج تعديلاً.

1. أنشئ `.env` على السيرفر (بجوار `docker-compose.yml`) يحوي القيم **الحالية نفسها**
   دون تغييرها بعد.
2. بدّل القيم في `docker-compose.yml` إلى `${MONGO_ROOT_PASSWORD}` / `${JWT_SECRET}` … إلخ.
3. أضف `.env.example` (بقيم وهمية) ليعرف أي مطوّر لاحق ما هي المتغيرات المطلوبة.
4. أعد النشر وتأكد أن كل شيء يعمل — **لا شيء تغيّر وظيفياً**، فقط مصدر القيمة.

### المرحلة ٣ — تدوير السرّين فعلياً (خارج ساعات العمل)

**أولاً `JWT_SECRET`** (الأسهل والأخطر):
- ولّد قيمة عشوائية قوية: `openssl rand -base64 48`
- ضعها في `.env`، أعد تشغيل الباك-إند.
- **النتيجة المتوقعة: الجميع يُخرجون.** أبلغ الفريق مسبقاً.

**ثم كلمة سر Mongo** (في نافذة منفصلة، وبعد التأكد من نجاح الأولى):
- غيّرها داخل Mongo نفسها، ثم في `.env`، ثم أعد التشغيل.
- **خذ نسخة احتياطية من قاعدة البيانات قبل هذه الخطوة.**

### المرحلة ٤ — تنظيف تاريخ Git (اختياري، قرار منفصل)

القيم القديمة تبقى في تاريخ الـ commits حتى بعد حذفها من الملف الحالي.
بعد المرحلة ٣ تصبح **قيماً ميتة لا قيمة لها**، وهذا كافٍ عملياً في مستودع خاص.
إعادة كتابة التاريخ (`git filter-repo`) عملية مزعجة وتكسر أي نسخ موجودة — لا أنصح بها
إلا إذا كان المستودع عاماً أو سيصبح كذلك.

---

## ٤. الترتيب الموصى به باختصار

1. **المرحلة ١ الآن** — بلا أثر على المستخدمين، وتُزيل أخطر ثغرة (الـ fallback الصامت).
2. **المرحلة ٢ قريباً** — بلا أثر وظيفي.
3. **المرحلة ٣ في وقت مخطط** — تُخرج الجميع؛ تحتاج إبلاغاً ونسخة احتياطية.
4. المرحلة ٤ — على الأرجح غير ضرورية.
